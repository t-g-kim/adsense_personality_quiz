# Cloudflare Pages Configuration
# This file tells Cloudflare how to build and deploy

[build]
# No build command needed - this is a static site
command = ""
# Serve files from root directory
directory = "/"

[build.environment]
# No environment variables needed
